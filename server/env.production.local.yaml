env:
  # root
  APP_ROOT_PASSWORD: ~
  APP_ROOT_EMAIL: admin@softvisio.net

  # smtp
  APP_SMTP_FROM: Arbitrator [devel] "no-reply@softvisio.net"
  APP_SMTP_HOSTNAME: smtp.gmail.com
  APP_SMTP_PORT: 465
  APP_SMTP_USERNAME: no-reply@softvisio.net
  APP_SMTP_PASSWORD: ~

  APP_SITE_URL: http://localhost/

  APP_DB: pgsql://<NAME>:<PASWORD>@pgsql
  APP_CLUSTER_ID: <NAME>
  APP_SERVICE_core: ws://core:8080
  # APP_SERVICE_datasets: ws://datasets:8080?cacheReset=disconnect&cacheReset=event/update
