dependencies:
  - "@softvisio/core"

services:
  api:
    components:
      webpack: true
      api: true
      rpc: true
      telegramSupportBot: true

defaultService: api

templates: ~

components:
  env:
    name: ~

  locales:
    locales: [en-GB, en-US, uk-UA, ru-RU]

config: ~
