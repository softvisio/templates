dependencies:
  - "@softvisio/core"

services:
  api:
    components:
      webpack: true
      api: true
      telegramNotificationsBot: true

defaultService: api

components:
  locales:
    locales: [en-GB, en-US, uk-UA, ru-RU]

config: ~
