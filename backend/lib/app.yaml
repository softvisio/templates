dependencies:
  - "@corejslib/core"

services:
  main:
    components:
      webpack: true
      api: true
      rpc: true
      telegramSupportBot: true

defaultService: main

templates: ~

components:
  env:
    name: ~

  locales:
    locales: [en-GB, en-US, uk-UA, ru-RU]

config: ~
