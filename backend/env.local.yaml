env: ~

components:
  dbh:
    uri: pgsql://test:1@devel

  # cluster:
  #   id: test

  # services:
  #   core: ws://devel:10000/api
  #   datasets: ws://devel:10001/api?clearCacheOn=disconnect,update

  api:
    root:
      email: zdm@softvisio.net
      telegramUsername: zdm001
      password: ~

    frontendUrl: http://localhost/

    passwordsStrength: weak

    notifications:
      smtp: smtps://no-reply@softvisio.net:password@smtp.gmail.com?from=Test
      telegramBotKey: ~
      firebase:
        prefix: ~
        serviceAccount: ~

    oauth:
      google:
        clientId: ~
        clientSecret: ~
      github:
        clientId: ~
        clientSecret: ~
      facebook:
        clientId: ~
        clientSecret: ~

config: ~
