env: ~

components:
  dbh:
    uri: postgresql://test:1@devel

  # cluster:
  #   id: test

  users:
    root:
      email: root@softvisio.net
      password: ~

    passwordsStrength: weak

  api:
    frontendUrl: https://local.softvisio.net/

    oauth:
      google:
        clientId: ~
        clientSecret: ~

  notifications:
    smtp: smtp+ssl://do.not.reply.to.this.address.001@gmail.com:<PASSWORD>@smtp.gmail.com?from=Test (localhost)

    telegramBot:
      apiToken: ~

    firebase:
      prefix: ~
      serviceAccount: ~

config: ~
